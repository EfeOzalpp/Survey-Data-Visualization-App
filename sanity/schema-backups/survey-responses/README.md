# Survey response schema archive

These historical Sanity schemas are retained to document and validate the
one-time migration of survey data to PostgreSQL. They are not registered in
the active Sanity Studio configuration.

PostgreSQL owns survey responses, solo-message edits, initial SSE history, and
live SSE changes. Sanity remains the CMS for general and personalized
gamification copy.
